1. Create a repository on Github called favorite-cmd-commands, make sure to check the checkmark about including a README file
( we can built git repo by our Mac terminal, (CLI) by using curl comannd and git API.  The syntax should be : 
           
             curl -u “erkmustech” http://api.github.com/user/repos -d  ‘{“name”:”repo-name” , “private”: true}’


                POST/user/

               GitHub API :  https://api.github.com/user"

curl \
  -X POST \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/user/repos \
  -d '{"name":"name"}'